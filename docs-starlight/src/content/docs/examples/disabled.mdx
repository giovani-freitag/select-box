---
title: Disabled
description: Dim individual options or entire groups — keyboard nav skips them, pointers can't commit them.
---

import Demo from "../../../components/demos/disabled/Demo.astro";

Set `disabled: true` on an option to keep it visible but uncommittable. When
every option in a group is disabled, the group header dims along with them
(keyboard nav skips the whole block).

<Demo />

## What "disabled" guarantees

- **Pointer clicks are ignored.** Hovering still highlights the row, but the
  controller's commit method short-circuits on disabled options.
- **Keyboard nav skips them.** `↓` / `↑` find the next enabled option in either
  direction; `Enter` on a disabled active option is a no-op.
- **Filtering still matches them.** A disabled row that matches the query stays
  visible (dimmed) — users can see *why* a value is unavailable rather than
  wondering whether they spelled it wrong.
- **Programmatic `setValue` works.** Disabled is a UI concern, not a data
  constraint — restoring persisted state still sets the value even if the option
  is currently disabled.

## Next examples

- [Search](../search/) — swap the default filter for fuzzy matching.
- [Large list](../large-list/) — 100 000 items, virtualized.
