---
title: Large list
description: Hundreds of thousands of items in a popover. Virtualization keeps the DOM under control.
---

import FrameworkDemo from "../../../components/FrameworkDemo.astro";
import LargeListControls from "../../../components/LargeListControls.astro";

Pass a huge array to `options` — the popover still opens instantly and
typing-to-filter stays responsive. Use the slider to regenerate the dataset
and feel the cost.

<LargeListControls />

<FrameworkDemo scenario="large-list">
    <Fragment slot="react-code">
        ```tsx
        import { SelectBox } from "@select-box/react";

        const critters = generate(100_000);

        <SelectBox
            options={critters}
            placeholder="Search the menagerie…"
        />;
        ```
    </Fragment>

    <Fragment slot="vue-code">
        ```vue
        <script setup lang="ts">
        import { SelectBox } from "@select-box/vue";

        const critters = generate(100_000);
        </script>

        <template>
            <SelectBox :options="critters" placeholder="Search the menagerie…" />
        </template>
        ```
    </Fragment>

    <Fragment slot="lit-code">
        ```html
        <select-box id="critters" placeholder="Search the menagerie…"></select-box>

        <script type="module">
            import "@select-box/lit/define";

            document.getElementById("critters").options = generate(100_000);
        </script>
        ```
    </Fragment>

    <Fragment slot="webcomponents-code">
        ```html
        <select-box id="critters" placeholder="Search the menagerie…"></select-box>

        <script type="module">
            import "@select-box/webcomponents/define";

            document.getElementById("critters").options = generate(100_000);
        </script>
        ```
    </Fragment>

    <Fragment slot="jquery-code">
        ```ts
        import "@select-box/jquery";
        import jQuery from "jquery";

        jQuery("#critters").selectBox({
            options: generate(100_000),
            placeholder: "Search the menagerie…",
        });
        ```
    </Fragment>
</FrameworkDemo>

## How it stays fast

- **Only visible rows are in the DOM.** A 240 px popover with 36 px rows keeps
  ~7 nodes live at any moment, regardless of the source array's size.
- **The filter walks the array linearly.** For hundreds of thousands of short
  strings, that's tens of milliseconds per keystroke — no throttling needed.
- **Keyboard nav scrolls into view in O(1).** Moving past the visible window
  jumps directly to the target offset, no DOM diffing.
