---
title: select-box
description: Framework-agnostic select-box. One core, five wrappers — React, Vue, Lit, Web Components, jQuery — all reading the same snapshot.
template: splash
pagefind: false
next: false
prev: false
---

import Hero from "../../components/Hero.astro";
import FrameworkChips from "../../components/FrameworkChips.astro";
import FeatureGrid from "../../components/FeatureGrid.astro";
import FrameworkDemo from "../../components/demos/grouped/Demo.astro";
import { withBase } from "../../lib/site-path";

<div class="sb-landing">

<Hero
    eyebrow="v0.1 — pilot · single-select"
    title="One core."
    titleHighlight="Every wrapper."
    tagline="A framework-agnostic core with thin wrappers for React, Vue, Lit, Web Components, and jQuery. Same snapshot fields, same keyboard behaviour, same ARIA wiring — pick the wrapper your stack already uses."
    primaryHref={withBase("/guides/getting-started/")}
    primaryLabel="Get started"
    secondaryHref={withBase("/examples/simple/")}
    secondaryLabel="Try it live"
>
    <FrameworkChips slot="footer" />
</Hero>

<section class="sb-landing-section not-content sb-landing-section--demo">
    <div class="sb-section-head not-content">
        <span class="sb-section-eyebrow">Live demo</span>
        <h2>The same component, five wrappers, one page.</h2>
        <p>Each tab below is the real wrapper rendering as an Astro Island — no iframe, no sandbox. Switch tabs; the behaviour doesn't.</p>
    </div>

    <FrameworkDemo>
        <Fragment slot="react-code">
            ```tsx
            import { SelectBox } from "@select-box/react";

            <SelectBox
                options={fruits}
                placeholder="Search fruits…"
                ungroupedLabel="Citrus"
                onChange={setCommitted}
            />
            ```
        </Fragment>

        <Fragment slot="vue-code">
            ```vue
            <script setup lang="ts">
            import { SelectBox } from "@select-box/vue";
            import { ref } from "vue";
            const committed = ref(null);
            </script>

            <template>
                <SelectBox
                    :options="fruits"
                    placeholder="Search fruits…"
                    ungrouped-label="Citrus"
                    @change="(v) => (committed = v)"
                />
            </template>
            ```
        </Fragment>

        <Fragment slot="lit-code">
            ```html
            <select-box id="fruit" placeholder="Search fruits…" ungrouped-label="Citrus"></select-box>

            <script type="module">
                import "@select-box/lit/define";

                const element = document.getElementById("fruit");
                element.options = fruits;
                element.addEventListener("change", (event) => {
                    console.log(event.target.value);
                });
            </script>
            ```
        </Fragment>

        <Fragment slot="webcomponents-code">
            ```html
            <select-box id="fruit" placeholder="Search fruits…" ungrouped-label="Citrus"></select-box>

            <script type="module">
                import "@select-box/webcomponents/define";

                const element = document.getElementById("fruit");
                element.options = fruits;
                element.addEventListener("change", (event) => {
                    console.log(event.target.value);
                });
            </script>
            ```
        </Fragment>

        <Fragment slot="jquery-code">
            ```ts
            import "@select-box/jquery";
            import jQuery from "jquery";

            jQuery("#fruit").on("change", (_event, value) => {
                console.log(value);
            });

            const box = jQuery("#fruit").selectBox({
                options: fruits,
                placeholder: "Search fruits…",
                ungroupedLabel: "Citrus",
            });
            ```
        </Fragment>
    </FrameworkDemo>
</section>

<section class="sb-landing-section not-content sb-landing-section--features">
    <div class="sb-section-head not-content">
        <span class="sb-section-eyebrow">What you get</span>
        <h2>The hard parts, already handled.</h2>
        <p>Big lists, search, keyboard, accessibility, native forms. The stuff every select-box should already solve — solved once, the same way in every wrapper.</p>
    </div>
    <FeatureGrid />
</section>

</div>
